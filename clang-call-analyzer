#!/usr/bin/env nix-shell
#!nix-shell -p python3 clang cmake --run python3

"""
Standalone runner for clang-call-analyzer.
This script can be run from anywhere and handles:
- NixOS shell activation
- Python module path resolution
- Command-line argument forwarding
"""

import sys
from pathlib import Path

# Add src/ to Python path
SCRIPT_DIR = Path(__file__).parent
sys.path.insert(0, str(SCRIPT_DIR / "src"))

# Import and run CLI
if __name__ == "__main__":
    from cli import main
    sys.exit(main())
